<UserControl
    x:Class="AnnoMapEditor.UI.Controls.MapView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:AnnoMapEditor.UI.Controls"
    mc:Ignorable="d">

    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Grid Name="canvasGrid" Grid.Row="0" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <Canvas Name="rotationCanvas" RenderTransformOrigin="0.5, 0.5" Width="0" Height="0">
                <Canvas.RenderTransform>
                    <TransformGroup>
                        <RotateTransform Angle="{Binding Source={x:Static local:MapView.MAP_ROTATION_ANGLE}}"/>
                    </TransformGroup>

                </Canvas.RenderTransform>
                <Canvas 
                Name="mapTemplateCanvas"
                Width="0"
                Height="0" />
                <Canvas
                Name="addIslandCanvas" />
            </Canvas>
        </Grid>
        <!--#B31E2328-->
        <Grid Grid.Row="1" HorizontalAlignment="Stretch" Height="100" Background="#7f000000">
        </Grid>
    </Grid>
</UserControl>
